name: Refresh profile metrics

on:
  push:
    branches: [main]
    paths:
      - "scripts/build_profile_metrics.py"
      - ".github/workflows/profile-metrics.yml"
  workflow_dispatch:
  schedule:
    # GitHub Actions cron runs in UTC. Minsk is UTC+3 year-round.
    - cron: "0 21 * * *"

permissions:
  contents: write

concurrency:
  group: profile-metrics
  cancel-in-progress: true

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"

      - name: Build contribution metrics
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PROFILE_USERNAME: Stellmaria
          PROFILE_TIMEZONE: Europe/Minsk
        run: python scripts/build_profile_metrics.py

      - name: Validate metrics SVG
        run: |
          python - <<'PY'
          import xml.etree.ElementTree as ET
          ET.parse("assets/profile-metrics-live.svg")
          print("profile-metrics-live.svg is valid XML")
          PY

      - name: Commit generated metrics
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/profile-metrics-live.svg
          if git diff --cached --quiet; then
            echo "Metrics are already up to date."
            exit 0
          fi
          git commit -m "chore(profile): refresh contribution metrics [skip ci]"
          git pull --rebase origin main
          git push origin HEAD:main
